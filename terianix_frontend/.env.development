# Local development — points the Ask AI fetch at the FastAPI dev server.
# The production value is set as a GitHub repo variable (VITE_API_URL)
# and injected at build time by deploy-swa.yml.
VITE_API_URL=http://localhost:8000
